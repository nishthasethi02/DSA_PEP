/*
Heaps -> A heap is a complete binary tree that follows the heap order property
    -> Complete Binary Tree(CBT) -> All levels are completely filled except the last level, the last level is filled from 
       left to right
    -> Heap Order Property -> 1. Max Heap -> Every parent node is greater than or equal to its children
                                            The largest element is always at root
                           -> 2. Min Heap -> Every parent node is smaller than or equal to its children
                                            The smallest element is always at top

    -> Heap is not a BST
*/

/* 
Priority Queue -> It is a special type of queue where elements are accessed on basis of priority, not the insertion order
               -> By default it has high
               -> internally it is 


*/